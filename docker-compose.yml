services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: root
      POSTGRES_DB: discover
    ports:
      - "5433:5432"
    volumes:
      - discover_pgdata:/var/lib/postgresql/data

volumes:
  discover_pgdata:
